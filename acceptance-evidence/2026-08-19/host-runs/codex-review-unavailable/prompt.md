$ppt-pilot

Create an auto-mode 6-slide internal orientation deck about how a fictional team should document handoffs. The audience is the fictional team itself and the desired action is to adopt a checklist. Use no network research and make no external factual claims. Use deck_id `acceptance-codex-review-unavailable`.

Acceptance harness constraint: this Codex process is launched with its stable `multi_agent` feature disabled. Complete and freeze the five manuscript files, then follow the installed Skill exactly. Independent review remains mandatory. If a fresh independent reviewer cannot be launched, record `review_unavailable` with the reason and stop. Do not use same-context self-review, do not pretend a review occurred, and do not create `theme.json`, samples, or SVG. Persist the state rather than merely describing it, then report the run directory and terminal stage.
