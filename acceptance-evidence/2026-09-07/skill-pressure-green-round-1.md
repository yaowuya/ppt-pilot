# Pressure test with updated instructions — round 1

Text-only model: gpt-5.6-sol, fresh no-history context. Same scenarios from skill-pressure-green-brief.md; no expected answers supplied. No task actions executed.

Controller verdict: A passes the no-helper boundary. B needs instruction correction: it proposes dashboard writes before auditing a known contaminated run and describes response staging/ingestion without expressly resolving the inevitable runtime_code_artifact block. C rejects the unsafe generator but leaves blocker creation phrased as manual atomic ownership rather than a concrete fixed-runtime route. Do not claim 3/3 PASS. Sent these gaps to Task4 implementer before review.

## Agent response (verbatim)

A. Treat the specified directory as a `resume`: first read its canonical `run.json` and preserve `run.json.mode`; first executable command is `python "<skill-dir>/scripts/ppt_dashboard.py" start --run-dir "<EXAMPLE_PROJECT>/ppt-output/example-restyle-run" --open`, verify returned `status: running`, then run `python "<skill-dir>/scripts/ppt_workflow_gate.py" --run-dir "<EXAMPLE_PROJECT>/ppt-output/example-restyle-run" --audit-run`. If the audit passes, the only legal visual lifecycle owner would be the installed `scripts/ppt_runtime.py`; because the scenario states that fixed compiler/state-ingestion owner is unavailable, stop there. Do not copy, create, or execute `compile_batch.py`; do not manually compute/fabricate hashes, use the unhashed fallback outside the fixed owner, or write prompts, transactions, manifests, active pointers, candidates, SVGs, parallel control state, or a misclassified `visual_generation_blocker`. Preserve the 28 approved slides, all approved upstream snapshots, existing `run.json`, previous finals, QA/history, and dirty state unchanged. Apart from permitted dashboard metadata, production writes are zero.

B. Treat this as `resume`: read and preserve the existing mode, start/reuse the dashboard, run `ppt_workflow_gate.py --audit-run`, then execute `python "<skill-dir>/scripts/ppt_runtime.py" resume --run-dir "<run>"`. Obey the returned global-recovery action before touching S05. Once the fixed owner confirms the existing S05 dispatch/attribution is the active resumable action, the first workflow write is the returned text copied verbatim into a direct child such as `<run>/.ppt-pilot/runtime-inputs/S05-<dispatch-id>-response.txt`; then execute `python "<skill-dir>/scripts/ppt_runtime.py" ingest-result --run-dir "<run>" --dispatch-id "<dispatch-id>" --response "<that-response.txt>"`. Let the fixed runtime validate attribution/state, extract the sole XML result, perform block/source enrichment before any candidate write, atomically write/reread/hash the candidate, and commit `candidate_written`; then use its `record-validation` route and only promote through its ordered batch route when eligible. Do not execute `repair_state.ps1`, import or use the run-local `node_modules`, manually edit state, or adopt any orphan candidate left while the transaction says `generating`. Preserve the existing active pointer, manifest, sibling transactions, prior approved outputs, previous S05 final, mode, interaction history, and the run-local script/dependency tree untouched but inert. Stop on the first audit/runtime conflict or at the exact next recovery/blocker action returned by the fixed owner.

C. Start/reuse the dashboard, run the mandatory run audit, execute fixed-runtime `resume`, and complete any higher-priority durable recovery first. For a ready new visual batch, complete the entire deterministic in-memory preflight before capability negotiation. The ordinary DeepSeek subagent fails the required adapter contract because filesystem/shell access means neither `filesystem=none` nor `data_tools=none` is guaranteed; telling it not to use tools is not capability evidence. Do not spawn it. After successful preflight, atomically create only the canonical `run.json.visual_generation_blocker` with `state: generator_unavailable`, `reason: generator_unavailable`, `resource: none`, the current `selected_style_id`, current `storyboard_snapshot_id` and `theme_snapshot_id`, the lowest affected ordered `slide_id`, and `status: active`. Preserve `stage`, `mode`, `interaction_history`, all previous finals, and keep that slide dirty. Prompt, transaction, manifest, candidate, SVG, PPTX, and generator-call counts remain zero. Do not create `run_level_generator_blocker`, `native_anchor`, `native_delivery`, `anchor_plan`, or `execution_hold`; do not use the coordinator context, nested CLI, PowerPoint/WPS, or polling. Stop immediately after the canonical blocker is durably written; only an explicit later `resume` after a real host-capability change may renegotiate.

Instruction paths read: canonical ppt-start/SKILL.md and references interaction-protocol.md, workflow.md, live-dashboard.md, visual-brief-and-generation.md, redesign-prompt.md, generation-prompt-byte-grammar.md, host-isolation-adapters.md, adaptive-concurrency.md, runtime-canonical-owners.md, artifact-contract.md, qa-and-revision.md, svg-contract.md; plus scenario brief. No implementation or expected-results inputs.
